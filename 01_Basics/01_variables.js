// alert("helo")
console.log("Hello");

const userId = 12345; // Can't change the value if using const

let classname = 5;  // mostly used variable

var username = "Farhan"
/*
Prefer not to use var
Because of issue in block scope and functional scope
*/
myNumber = "nahidunga"; ; // variables can also be declared like this 

// userId = "4545"   // not allowed 

console.log(userId);

console.table([userId, username, classname, myNumber]);